---
import Layout from '../layouts/Layout.astro';
import backgroundImageDesktop from '../assets/background-desktop.png';
import backgroundImageMobile from '../assets/background-mobile.png'
import backgroundImageTablet from '../assets/background-tablet.png'
import linkedInLogo from '../assets/linkedin-logo.png';
import githubLogo from '../assets/github-logo.svg';
---

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<!--
TODO:
* Clean up fonts
* Optimize image
* Get rid of tiny bit of scroll
* Get image working on all media types
  * iPhone SE
* Nail down actual content
-->
<link href="https://fonts.googleapis.com/css2?family=Lato:ital,wght@0,100;0,300;0,400;0,700;0,900;1,100;1,300;1,400;1,700;1,900&family=Lora:ital,wght@0,400..700;1,400..700&family=Merriweather:ital,wght@0,300;0,400;0,700;0,900;1,300;1,400;1,700;1,900&family=Montserrat:ital,wght@0,100..900;1,100..900&family=Raleway:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
<Layout title="Welcome to Astro.">
    <div class="relative w-screen container image-container">
        <!--<img src={backgroundImageMobile.src}-->
        <!--     sizes="(max-width: 480px) 698px, (min-width: 640px) 1792px, 698px"-->
        <!--     srcset={`${backgroundImageMobile.src} 698w, ${backgroundImage.src} 1792w`}-->
        <!--     alt="background of me" class="bg-cover w-full object-cover bg-image" />-->
        <img src={backgroundImageTablet.src}
             alt="hi" class="full-screen-background bg-cover"/>
    </div>
</Layout>

<style>
    body {
        //font-family: 'Lora', serif;
        font-family: 'Merriweather', serif;
        -webkit-font-smoothing: antialiased;
        color: #ffe500;
        text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2); /* Soft shadow for readability */
        color: #fff;
        //-webkit-text-stroke: 1px black;
        //paint-order: stroke fill;
        text-rendering: optimizeLegibility;
    }

    .text-my-name {
        font-family: 'Lato', sans-serif;
    }

    .image-container {
        position: relative;
        display: inline-block; /* Adjust according to your layout */
    }

    .image-container img {
        display: block;
        width: 100%; /* Adjust according to your needs */
        height: auto; /* Adjust according to your needs */
    }

    .image-container .horizontal-scrim {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(to right, rgba(0, 0, 0, .3), rgba(0, 0, 0, 0));
        pointer-events: none; /* Allows clicks to pass through to the image if needed */
    }

    .image-container .vertical-scrim {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(to bottom, rgba(0, 0, 0, .15), rgba(0, 0, 0, 0));
        pointer-events: none; /* Allows clicks to pass through to the image if needed */
    }


    a.clickable-image {
        padding: 5px; /* Padding to ensure background is visible */
    }

    a.clickable-image:hover {
        background-color: rgba(211, 211, 211, 0.3); /* Light gray background with slight transparency */
        display: inline-block;
    }

    .about-me-text {
        //letter-spacing: 0.5px; /* Slight letter spacing for readability */
    }


    .container {
        max-width: 100%
    }

    html, body {
        height: 100%;
        margin: 0;
        overflow: hidden; /* Prevents scrolling */
    }


    .full-screen-background {
        position: fixed; /* Fix the element in place */
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-image: url('your-image.jpg'); /* Replace with your image URL */
        background-size: cover; /* Ensures the image covers the entire element, zooming if necessary */
        background-position: center; /* Centers the image */
        background-repeat: no-repeat; /* Prevents the image from repeating */
        z-index: -1; /* Places the background behind other content */
    }

</style>
